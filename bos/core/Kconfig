#版本配置，软硬件版本
config HW_VERSION
    int "Hardware version"
    default 211212

config FW_VERSION
    int "Firmware version"
    default 80303

config FW_NAME
    string "Firware Name"
    default "BabyOS"

config _QUEUE_ENABLE
    bool "queue enable"
    default n

config _SEM_ENABLE
    bool "semaphore enable"
    default n      

    